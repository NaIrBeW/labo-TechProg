Réponse :

L'algorithme représenté est de complexité O(n+Range). Dans cet algorithme, nous pouvons y voir 3 boucle for de la même valeur et ou longueur. Par contre, une seule boucle change. L'algorithme semble faire un quicksort, qui va organisé le tableau arr. La première boucle semble servir a compter le nombre d'éléments que contient le tableau, ensuite la seconde boucle calcul jusqu'à ce que i = au range le  nombre de count qu'il faut faire. Puis les deux dernières boucle semble calculer le nombre de boucle qu'il faut faire pour output les nombres en ordre en partant de la droite vers la gauche, selon moi puisque count fait --. Ici, l'algorithme a une nécessité que l'ont parcours l'ensemble des données , donc puisque les éléments ne sont pas trié et que la recherche d'élément spécifique est nécessaire, nous faisons un algorithme O(n+Range). 